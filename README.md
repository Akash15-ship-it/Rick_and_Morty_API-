Develop a backend for Characters, Episodes, and Locations. Implement
CRUD operations, search characters by name, filter by status/species,
sort and paginate results, add JWT authentication, and follow MVC
architecture.
